# 拉取远端镜像并打包成 zip 的脚本

# usage

## save

- `chmod +x ./save-images.sh`

- `sudo ./save-images.sh --image-list ./images.txt`

## load && push

- `chmod +x ./load-images.sh`

- `sudo ./load-images.sh --image-list ./images.txt --registry 127.0.0.1:5000`
